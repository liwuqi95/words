<%= form_with(model: word_library, local: true) do |form| %>
  <% if word_library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word_library.errors.count, "error") %> prohibited this word_library from being saved:</h2>

      <ul>
      <% word_library.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
